<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sist_log">  
  <insert id="insert">
  	insert into sist_log values(seq_sist_log.nextval,#{uri},#{time},#{stay},#{ip})
  </insert>
</mapper>